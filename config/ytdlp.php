<?php

return [
    /*
    |--------------------------------------------------------------------------
    | yt-dlp binary path
    |--------------------------------------------------------------------------
    | Set YT_DLP_PATH in your .env, or install yt-dlp globally so "yt-dlp"
    | resolves in the system PATH. On most systems after `pip install yt-dlp`
    | the binary will be available as just "yt-dlp".
    |
    */
    'binary' => env('YT_DLP_PATH', 'yt-dlp'),

    /*
    |--------------------------------------------------------------------------
    | Max concurrent downloads
    |--------------------------------------------------------------------------
    */
    'max_concurrent' => env('YT_DLP_MAX_CONCURRENT', 3),

    /*
    |--------------------------------------------------------------------------
    | Download timeout (seconds)
    |--------------------------------------------------------------------------
    */
    'timeout' => env('YT_DLP_TIMEOUT', 3600),

    /*
    |--------------------------------------------------------------------------
    | Supported formats exposed in the UI
    |--------------------------------------------------------------------------
    */
    'formats' => ['mp4', 'webm', 'mkv', 'audio'],

    /*
    |--------------------------------------------------------------------------
    | Supported resolutions exposed in the UI
    |--------------------------------------------------------------------------
    */
    'resolutions' => [360, 480, 720, 1080, 2160],
];
